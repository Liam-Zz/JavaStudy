package com.liam.oop.elementary.variableparameter;

public class Demo {
    /*
        可变参数
        java 允许将同一个类多个同名,同功能但参数个数不同的方法,封装成一个方法.

        可变参数细节
            1.可变参数的实参可以为 0 或任意多个;
            2.可变参数的实参可以为数组;
            3.可变参数的本质就是数组
            4.可变参数可以和普通参数一起放在形参列表,但必须保证可变参数在最后;
            5.一个形参列表最多存在一个可变参数
     */
}
