<?xml version="1.0" encoding="UTF-8" ?>
<!--
1.跟标签只能有一个
2.第一行永远都是“<?xml version="1.0" encoding="UTF-8" ?>” 前面不可加任何字符
3.XML 是由约束的，约束用于限定XML内部能编写的内容
4.XML约束方式实现
    dtd约束：上手快、语法简单，约束没schema细致 后缀：dtd
    schema约束：上手慢、语法复杂，约束比dtd细致 后缀：xsd
    约束代码自动生成（相当于友好提示）
    框架会自动解析XML
-->
<jdbc>
    <liam id="001">
        <username>root</username>
        <password>root</password>
        <driver>com.mysql.cj.jdbc.Driver</driver>
        <url>jdbc:mysql://localhost:3306/javaliam</url>
    </liam>
    <div id="002">
        <username>root</username>
        <password>root</password>
        <driver>com.mysql.cj.jdbc.Driver</driver>
        <url>jdbc:mysql://localhost:3306/javadiv</url>
    </div>
    <test id="003">
        <username>root</username>
        <password>root</password>
        <driver>com.mysql.cj.jdbc.Driver</driver>
        <url>jdbc:mysql://localhost:3306/javatest</url>
    </test>
</jdbc>